services:
  album-tracker:
    build: .
    ports:
      - "4180:4180"
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
      - TZ=Europe/London
    env_file: .env
    restart: unless-stopped
