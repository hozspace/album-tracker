# album-tracker

Self-hosted album diary + recommendations.

## Dev

```sh
npm install
npm run dev
```

Client on http://localhost:5173, API on http://localhost:4180.

## Docker

```sh
docker compose up -d
```

Serves everything on http://localhost:4180. SQLite data persists in `./data`.
